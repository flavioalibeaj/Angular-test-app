import { UpperLowerPipe } from './upper-lower.pipe';

describe('UpperLowerPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperLowerPipe();
    expect(pipe).toBeTruthy();
  });
});
