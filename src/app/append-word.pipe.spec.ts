import { AppendWordPipe } from './append-word.pipe';

describe('AppendWordPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendWordPipe();
    expect(pipe).toBeTruthy();
  });
});
